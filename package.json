{
  "name": "cryc",
  "author": "Aleph Retamal",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "build:use-contract-tx": "lerna run build --scope @cryc/use-contract-tx",
    "watch:use-contract-tx": "lerna run watch --scope @cryc/use-contract-tx",
    "package:use-contract-tx": "lerna exec --scope @cryc/use-contract-tx -- npm pack",
    "build:use-ens-profile": "lerna run build --scope @cryc/use-ens-profile",
    "watch:use-ens-profile": "lerna run watch --scope @cryc/use-ens-profile",
    "package:use-ens-profile": "lerna exec --scope @cryc/use-ens-profile -- npm pack"
  },
  "devDependencies": {
    "@types/react": "^17.0.6",
    "@types/react-dom": "^17.0.5",
    "@types/node": "^17.0.23",
    "lerna": "^4.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "typescript": "^4.6.3"
  }
}
